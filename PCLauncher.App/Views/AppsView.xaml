<UserControl x:Class="PCLauncher.App.Views.AppsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <Border Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="16" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Top Bar: Title, Search, Filter & Add Button -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Kayıtlı Programlar" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF" />
                    <TextBlock x:Name="AppsSummaryText" Text="Telefonunuzda listelenecek uygulamaları yönetin." FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" />
                </StackPanel>

                <!-- Search & Category Filters -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,14,0">
                    <TextBox x:Name="SearchBox" Width="180" Padding="8,6" TextChanged="SearchBox_TextChanged" Margin="0,0,8,0" />
                    <ComboBox x:Name="CategoryFilterCombo" Width="130" SelectionChanged="CategoryFilterCombo_SelectionChanged" />
                </StackPanel>

                <!-- Add Button -->
                <Button Grid.Column="2" Content="➕ Program Ekle" Style="{StaticResource PrimaryButton}" Click="AddApp_Click" Padding="16,10" />
            </Grid>

            <!-- Main Content List or Empty State -->
            <Grid Grid.Row="2">
                <!-- Empty State -->
                <Border x:Name="EmptyStateBorder" Style="{StaticResource CardPanel}" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="40" Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="🎮" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12" />
                        <TextBlock Text="Henüz Tanımlı Program Yok" FontSize="18" FontWeight="Bold" Foreground="#FFFFFF" HorizontalAlignment="Center" Margin="0,0,0,8" />
                        <TextBlock Text="Telefonunuzdan başlatmak istediğiniz Windows programlarını ekleyin." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,12" />
                        <Button Content="➕ İlk Programı Ekle" Style="{StaticResource PrimaryButton}" Click="AddApp_Click" HorizontalAlignment="Center" Padding="16,10" />
                    </StackPanel>
                </Border>

                <!-- Apps List -->
                <ListBox x:Name="AppsListBox" Background="Transparent" BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,0,0,10" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource BgCardBrush}" BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1" CornerRadius="12" Padding="14">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="14" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Icon -->
                                    <Border Width="48" Height="48" Background="#111624" BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1" CornerRadius="8">
                                        <Image Source="{Binding DisplayIcon}" Width="36" Height="36" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality" />
                                    </Border>

                                    <!-- App Info -->
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                            <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,8,0" />
                                            <Border Background="#222B42" CornerRadius="4" Padding="6,2" VerticalAlignment="Center" Margin="0,0,8,0">
                                                <TextBlock Text="{Binding Category}" FontSize="11" Foreground="#93C5FD" FontWeight="SemiBold" />
                                            </Border>
                                            <TextBlock Text="⭐" Visibility="{Binding FavoriteVisibility}" FontSize="12" VerticalAlignment="Center" />
                                        </StackPanel>

                                        <TextBlock Text="{Binding ExePath}" FontSize="11" Foreground="{StaticResource TextMutedBrush}" TextTrimming="CharacterEllipsis" MaxWidth="450" Margin="0,0,0,4" />

                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Visibility="{Binding DescriptionVisibility}" Margin="0,0,10,0" />
                                            <TextBlock Text="{Binding LaunchStatsText}" FontSize="11" Foreground="#10B981" />
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Action Buttons -->
                                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="▶ Test Et" Style="{StaticResource SuccessButton}" Click="TestLaunch_Click" Tag="{Binding Id}" ToolTip="PC'de hemen çalıştırıp test et" Margin="0,0,8,0" />
                                        <Button Content="✏ Düzenle" Style="{StaticResource ModernButton}" Click="EditApp_Click" Tag="{Binding Id}" Margin="0,0,8,0" />
                                        <Button Content="🗑 Sil" Style="{StaticResource DangerButton}" Click="DeleteApp_Click" Tag="{Binding Id}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Grid>
    </Border>
</UserControl>
